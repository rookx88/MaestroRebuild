# Required API Keys
LANGCHAIN_API_KEY=your_langchain_api_key_here  # For LangChain tracing and services
OPENAI_API_KEY=your_openai_api_key_here  # For GPT-4o model access

# Encryption Configuration
# Generate with:
# python -c 'import base64, os
# from cryptography.hazmat.primitives.ciphers.aead import AESGCM
# print(base64.urlsafe_b64encode(AESGCM.generate_key(bit_length=256)).decode())'
ENCRYPTION_KEY=your_encryption_key_here

# Application Settings
APP_ENV=development  # [development|staging|production]
DEBUG=False  # Set to True for verbose output
PORT=5000  # Default port for local development 
